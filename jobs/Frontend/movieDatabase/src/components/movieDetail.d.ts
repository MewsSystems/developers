import { FC } from "react";
import { MovieDetails } from "../types/movies";
interface MovieDetailProps {
    movie: MovieDetails;
}
export declare const MovieDetail: FC<MovieDetailProps>;
export {};
