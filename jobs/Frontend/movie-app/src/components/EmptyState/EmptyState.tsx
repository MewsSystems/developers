import React from "react";
import { StyledEmptyState } from "./EmptyState.styles";

const EmptyState = () => {
  return <StyledEmptyState>No movies to display</StyledEmptyState>;
};

export default EmptyState;
