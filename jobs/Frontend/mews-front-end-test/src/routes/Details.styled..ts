import styled from 'styled-components';
import { Link } from 'react-router-dom';

const StyledParagraph = styled.p`
  margin-bottom: 0.5rem;
`;

const StyledLink = styled(Link)`
  color: white;
`;

export { StyledParagraph, StyledLink };
