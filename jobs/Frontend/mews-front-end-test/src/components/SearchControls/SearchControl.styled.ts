import styled from 'styled-components';

const StyledLegend = styled.legend`
  position: absolute;
  overflow: hidden;
  height: 1px;
  width: 1px;
  margin: -1px;
  padding: 0;
`;

export { StyledLegend };
