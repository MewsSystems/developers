import styled from 'styled-components';

export const StyledWrapperListMovies = styled.div`
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
  gap: 24px;
  justify-items: center;
  padding: 20px;
  width: 100%;
`;
