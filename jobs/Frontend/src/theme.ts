import { DefaultTheme } from "styled-components";

const theme: DefaultTheme = {
  colors: {
    primary: "#41d3be", // light green
    secondary: "white",
    background: "#333", // dark grey
    link: "#70757a", // grey
  },
};

export default theme;
