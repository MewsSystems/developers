import styled from 'styled-components';
import Box from '@/components/Box';

const SiteBackground = styled(Box)`
  background-image: linear-gradient(to top, #e6e9f0 0%, #eef1f5 100%);
`

export default SiteBackground;
