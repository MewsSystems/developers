import styled from 'styled-components';

export const MovieTitle = styled.span`
  white-space: nowrap;
  max-width: 100%;
  text-overflow: ellipsis;
  overflow: hidden;
  padding: 4px 8px;
  text-align: center;
  font-size: 0.75rem;
`;
