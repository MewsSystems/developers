import { Color } from './color';

export const enum Gradient {
    AccentsLinearTitled = `linear-gradient(286deg, ${Color.Accent} 0%, ${Color.SecondaryAccent} 100%)`,
    FadeTop = 'linear-gradient(transparent, rgb(0 0 0) 45%)'
}