{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "TelemetrySettings": {
    "TelemetryEndpointUrl": "http://seq:5341/ingest/otlp/v1/logs",
    "TelemetryEndpointApiKey": "[Not needed for this exercise and would not be stored here anyway (I would load it from a secure store/user secrets)]",
    "TelemetryEndpointUseAuthentication": false
  }
}
