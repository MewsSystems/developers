import { MovieSearch } from '@/features/MovieSearch/MovieSearch'
import { Layout } from '@/components/Layout'

export const Home = () => {
  return (
    <Layout>
      <MovieSearch />
    </Layout>
  )
}
