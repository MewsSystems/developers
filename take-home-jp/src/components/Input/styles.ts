import { colors } from '../../styling/colors';

export const styles = {
  input: {
    display: 'flex',
    borderRadius: '5px',
    backgroundColor: colors.whiteSmoke,
    border: `1px solid ${colors.gray}`,
  },
};
