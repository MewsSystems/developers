import { colors } from '../../styling/colors';

export const styles = {
  buttonItems: {
    '& .MuiPaginationItem-root': {
      color: colors.whiteSmoke,
    },
  },
  container: {
    display: 'flex',
    justifyContent: 'center',
  },
};
