import { strict } from "assert";

interface Currency {
  name: string;
  code: string;

  /* constructor(name: string, code: string) {
    this.name = name;
    this.code = code;
  } */
}
export default Currency;
