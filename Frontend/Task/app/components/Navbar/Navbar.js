import React from 'react';
import styles from 'Components/Navbar/Navbar.module.css';

const Navbar = () => (
  <nav className={styles.nav}>
    <h3 className={styles.text}>currency rates</h3>
  </nav>
);

export default Navbar;
