import actionTypes from './action-types'
import fetchState from './fetch-state'
import trend from './trend'
import { TURQUOISE, PURPLE, PINK } from './color'
import { CONFIG_URL, RATES_URL, UPDATE_INTERVAL } from './server'

export {
  actionTypes,
  fetchState,
  trend,
  TURQUOISE,
  PINK,
  PURPLE,
  UPDATE_INTERVAL,
  RATES_URL,
  CONFIG_URL,
}
