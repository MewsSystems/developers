export const theme = {
  baseSpacing: 15,
  containerWidth: 500,
  minWidth: 320,
  borderColor: "#f0f0f0",
  emphColor: "crimson",
  transitionDuration: 300
};

export type ThemeType = typeof theme;
